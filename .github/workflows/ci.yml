name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      - name: 安装 uv 与 Python 3.12
        uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
      - name: 安装项目与开发依赖
        run: uv sync --extra dev --locked
      - name: 运行 Ruff 静态检查
        run: uv run ruff check src tests
      - name: 运行测试与覆盖率检查
        run: uv run pytest --cov=wecom_ai_gateway --cov-report=term-missing
      - name: 安装并测试可选 ClawBot Bridge
        run: |
          cd bridge
          uv sync --extra dev
          uv run ruff check src tests
          uv run pytest
      - name: 构建容器镜像
        run: docker build -t multi-channel-ai-gateway:test .
      - name: 构建 ClawBot Bridge 镜像
        run: docker build -t multi-channel-clawbot-bridge:test bridge
